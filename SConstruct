# -*- mode: python; -*-

# This SConscript describes build rules for the "samovar" project.

import itertools
import os
import re
import sys

SConscript('example/SConstruct')
SConscript('test/SConstruct')